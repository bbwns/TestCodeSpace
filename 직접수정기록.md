# 🛠️ 직접 수정 기록 (나의 실습 일지)

> 내가 직접 코드/DB를 수정해본 내용을 기록한다.
> "AI가 해준 것"이 아니라 **"내가 직접 해본 것"** 만 기록!

---

## 2026-08-05 — 고아 파일을 DB에 등록하기

### 🎯 상황 (왜 했나)
- `uploads/` 폴더에 이미지 **파일 4개**가 있는데, DB(`backup.db`)에는 **정보가 없었음**
- 갤러리는 DB 정보를 보고 그림을 그리므로 → **파일만 있고 정보가 없으면 화면에 안 보임**
- 이렇게 "파일은 있는데 정보가 없는" 상태를 **고아 파일(orphan file)** 이라고 함

### 🧠 배운 개념
> **이미지 파일과 이미지 정보는 저장 장소가 다르다.**
> - 파일 → `uploads/` 폴더
> - 정보 → `backup.db` 표
> 갤러리는 **정보(DB)** 를 보고 그리므로, 파일만 있으면 안 보인다!

### ✍️ 내가 직접 한 것
`INSERT INTO` SQL로 파일 4개를 DB에 한 줄씩 추가했다.

```js
const sqlite3 = require('sqlite3').verbose();
const db = new sqlite3.Database('backup.db');

const files = [
  ['13d91bfb3816299be15eecd010671569', '13d91bfb3816299be15eecd010671569.webp', '이미지 1'],
  ['465cf54a585ebcf4003fd5c6cb0c4c04', '465cf54a585ebcf4003fd5c6cb0c4c04.webp', '이미지 2'],
  ['b00d10d2ba9850c20c451a700feb04d7', 'b00d10d2ba9850c20c451a700feb04d7.webp', '이미지 3'],
  ['d66e5763b4c2465f25f28b5b7091175a', 'd66e5763b4c2465f25f28b5b7091175a.webp', '이미지 4']
];

files.forEach(f => {
  db.run('INSERT INTO images (filename, original_name, title, description) VALUES (?, ?, ?, ?)',
    [f[0], f[1], f[2], '기존 파일 복구'],
    function(err) {
      if (err) console.log('에러:', err.message);
      else console.log('등록됨 id=' + this.lastID + ' : ' + f[2]);
    });
});
```

### ✅ 결과
- DB에 id 2~5번으로 이미지 4개가 등록됨
- 이제 서버를 켜면 갤러리에 이미지 4개가 보임

### 📌 메모
- `this.lastID` = 방금 INSERT한 행의 id 번호
- `?` 플레이스홀더로 값 전달 (SQL 인젝션 방지)
- 파일명은 확장자가 없어서, 원본이름에 `.webp`를 붙여 표시용으로 사용

---

## 2026-08-05 — 갤러리가 안 보이던 문제 고치기

### 🎯 상황 (왜 했나)
- 이미지를 업로드했는데 갤러리(`<div id="gallery">`)가 **비어서** 아무것도 안 보였음

### 🤔 내 추측 (처음에 이렇게 생각했다)
- **내 생각**: "애초에 프론트(브라우저) 문제일 것 같다"
- **근거**: 서버(백엔드)는 계속 "됐다"고 하는데 화면이 안 보이니까, 문제는 화면을 그리는 쪽(프론트)일 거라고 생각함
- **결과**: ✅ **추측이 맞았음!** 실제로 `index.html`의 자바스크립트에 문제가 있었음

### 🧠 배운 개념
> **괄호 하나가 잘못되면 전체가 깨진다!**
> 자바스크립트는 괄호 `( )`, 중괄호 `{ }`가 **짝이 맞아야** 동작한다.
> 하나가 많거나 적으면 **구문 에러(Syntax Error)** → **스크립트 전체가 실행 안 됨**

### ✍️ 내가 직접 고친 것
`public/index.html`의 스크립트 끝부분에서 **`});` 가 하나 더 많았던 것**을 제거했다.

### ✅ 결과
- 괄호 짝을 맞추니 자바스크립트가 정상 실행됨
- 갤러리에 이미지가 보임!

### 📌 메모
- 서버(API)는 정상인데 화면만 안 보이면 → **프론트(브라우저) 문제**일 가능성이 높음
- **추측 → 확인 → 결론** 순서로 기록하면, 내 사고 과정을 돌아볼 수 있어서 좋다
