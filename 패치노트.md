# 📦 패치노트 (변경 기록)

> 기능을 추가/수정할 때마다 날짜와 함께 기록한다.
> 최신 내용이 위로 온다.

---

## 2026-08-06 — 🔍 이미지 상세 페이지 추가

### ✨ 추가된 것
- 갤러리의 이미지를 **클릭하면 상세 페이지로 이동**
- 상세 페이지(`detail.html`)에서 이미지를 **크게** 보고, 제목/설명/업로드 시각 확인
- "← 목록으로 돌아가기" 링크로 목록 복귀

### 🆕 새로 배운 것 (함수.md 참고)
| 개념 | 함수.md |
|------|---------|
| 페이지 라우트 vs 데이터 API (HTML vs JSON) | - |
| `app.get('/image/:id')` (상세 페이지 라우트) | - |
| `app.get('/api/images/:id')` (상세 데이터 API) | - |
| `db.get` (한 줄만 읽기) | L3 |
| `location.pathname.split('/').pop()` (주소에서 id 꺼내기) | - |
| `location.href` (페이지 이동) | - |

### 🔧 바뀐 파일
- `server.js` — 상세 페이지 라우트 + 상세 데이터 API 추가
- `public/detail.html` — 새로 만든 상세 페이지
- `public/index.html` — 이미지 클릭 시 상세 페이지로 이동

### 🧠 오늘의 핵심
> **"화면(HTML)을 보여주는 라우트"와 "데이터(JSON)를 주는 라우트"는 서로 다르다.**
> 상세 페이지는 빈 화면(HTML)을 받고, 그 안의 JS가 다시 데이터 API를 호출해 화면을 채운다.

---

## 2026-08-05 — 🗑️ 이미지 삭제 기능 추가

### ✨ 추가된 것
- 갤러리의 각 이미지에 **🗑️ 삭제 버튼** 추가
- 삭제 시 **파일(`uploads/`)과 DB 정보 둘 다** 삭제 (데이터 일관성)
- 삭제 전 **확인창**("정말 삭제할까요?") 표시

### 🆕 새로 배운 것 (함수.md 참고)
| 개념 | 함수.md |
|------|---------|
| `app.delete` (DELETE 요청) | L1 |
| `req.params.id` (주소 값 읽기) | L2 |
| `db.get` (한 줄만 읽기) | L3 |
| `DELETE FROM ... WHERE` (한 줄 삭제) | L4 |
| `fs.unlink` (파일 삭제) | L5 |
| `ENOENT` (파일 없음 에러) | L6 |
| `fetch(..., { method: 'DELETE' })` | L7 |
| `addEventListener('click')` | L8 |
| `dataset.id` (숨긴 값 읽기) | L9 |
| `confirm()` (확인창) | L10 |
| `location.reload()` (새로고침) | L11 |

### 🔧 바뀐 파일
- `server.js` — 삭제 API 라우트 추가
- `public/index.html` — 삭제 버튼 + JS 로직 추가
- `함수.md` — L 섹션 (삭제 기능) 추가

### 🧠 오늘의 핵심
> **삭제도 파일 + DB 둘 다 해야 한다.** 하나만 지우면 어긋남(불일치)이 생긴다.
