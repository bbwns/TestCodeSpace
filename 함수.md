# 📚 함수 사전 (나만의 참고서)

> 새로운 함수가 나올 때마다 여기에 하나씩 추가한다.
> 채팅에서는 길게 설명하지 않고 "함수.md N번 참고"로 안내!

## 🗂️ 목차

- A. 라이브러리 (설치한 것들)
- B. 서버 만들기 & 실행
- C. 라우트 (요청 처리)
- D. 응답 보내기 (res)
- E. 요청 받기 (req)
- F. 파일 업로드 (multer)
- G. 데이터베이스 (sqlite3)
- H. SQL 문법
- I. 파일/경로 (Node 기본)
- J. 프론트엔드 (브라우저 JS)
- K. HTML 문법 (브라우저 화면)
- L. 삭제 기능 (DELETE)
- M. 상세 페이지 (페이지 이동 & 주소 값)

---

## A. 라이브러리 (설치한 것들)

| 이름 | 뭐 하는 것 | 비유 |
|------|-----------|------|
| `express` | 웹 서버를 쉽게 만드는 도구 | 레스토랑 건물+직원 관리 시스템 |
| `sqlite3` | DB(엑셀 표)를 파일로 다루는 도구 | 엑셀 파일을 여는 프로그램 |
| `multer` | 파일 업로드를 처리하는 도구 | 우체부 (파일 받아서 보관) |
| `path` | 파일 경로(주소)를 다루는 도구 | 길찾기 도우미 |
| `fs` | 파일/폴더를 만들고 지우는 도구 | 컴퓨터 파일 관리자 |

### 사용 예 (server.js 맨 위)
```js
const express = require('express');
const sqlite3 = require('sqlite3').verbose();
const multer = require('multer');
const path = require('path');
const fs = require('fs');
```

---

## B. 서버 만들기 & 실행

### B1. `express()`
- **뭐 하는 함수?**: 웹 서버 객체를 만든다
- **비유**: 레스토랑 건물을 짓는 것
```js
const app = express();
```

### B2. `app.listen(포트, 콜백)`
- **뭐 하는 함수?**: 서버를 실행해서 요청을 기다린다
- **비유**: 레스토랑 문을 열고 영업 시작
```js
app.listen(3000, () => {
  console.log('서버 실행 중: http://localhost:3000');
});
```

---

## C. 라우트 (요청 처리)

### C1. `app.get('경로', 핸들러)`
- **뭐 하는 함수?**: "이 주소로 GET(조회) 요청이 오면 이 동작을 해줘"
- **비유**: 메뉴판. "/" 메뉴를 시키면 이 요리(응답)를 줌
```js
app.get('/', (req, res) => {
  res.sendFile(path.join(__dirname, 'public/index.html'));
});
```

### C2. `app.post('경로', 미들웨어, 핸들러)`
- **뭐 하는 함수?**: "이 주소로 POST(보내기) 요청이 오면 이 동작을 해줘"
- **비유**: 손님이 음식을 배달로 보냈을 때(업로드) 받아서 처리
```js
app.post('/upload', upload.single('image'), (req, res) => {
  // 파일 저장 + DB 저장
});
```

### C3. `app.use(미들웨어)`
- **뭐 하는 함수?**: 모든 요청에 공통으로 거치는 단계(미들웨어)를 등록
- **비유**: 레스토랑 입구의 안내 데스크
```js
app.use(express.static('public'));        // 정적 파일 서빙
app.use('/uploads', express.static('uploads'));  // 업로드 폴더 공개
app.use((err, req, res, next) => { ... });       // 에러 핸들러
```

### C4. `express.static('폴더명')`
- **뭐 하는 함수?**: 폴더 안의 파일을 그대로 공개(서빙)
- **비유**: 창고를 열어두고 손님이 직접 꺼내보게 함
```js
app.use(express.static('public'));
```

---

## D. 응답 보내기 (res)

| 함수 | 뭐 하는 것 | 비유 |
|------|-----------|------|
| `res.sendFile(경로)` | HTML 파일 통째로 보내기 | 완성된 문서를 건네줌 |
| `res.send(문자열)` | 텍스트 보내기 | 쪽지 전달 |
| `res.json(객체)` | JSON(딕셔너리 같은 것)으로 보내기 | 데이터 서류 전달 |
| `res.redirect('/')` | "다른 페이지로 이동시켜라" | "저쪽 창구로 가세요" |
| `res.status(400)` | 상태 코드 지정 (400=잘못된 요청) | "불량" 도장 찍기 |

```js
res.sendFile(path.join(__dirname, 'public/index.html'));
res.json(rows);          // DB에서 읽은 목록을 JSON으로
res.redirect('/');       // 성공 시 메인으로
res.status(400).send('이미지 파일을 선택해 주세요!');
```

> 상태 코드: 200=성공, 400=잘못된 요청, 404=없음, 500=서버 에러

---

## E. 요청 받기 (req)

| 표현 | 뜻 | 예시 |
|------|-----|------|
| `req.body.title` | 폼에서 보낸 "title" 값 | `const title = req.body.title` |
| `req.file` | 업로드된 파일 정보 (multer가 넣어줌) | `if (!req.file)` |
| `req.file.filename` | 서버에 저장된 파일명 | DB에 저장 |
| `req.file.originalname` | 사용자가 올린 원본 파일명 | DB에 저장 |
| `req.file.mimetype` | 파일 종류 (예: image/png) | 필터링에 사용 |

```js
if (!req.file) {
  return res.status(400).send('이미지 파일을 선택해 주세요!');
}
const title = req.body.title || '제목 없음';
```

---

## F. 파일 업로드 (multer)

### F1. `multer({ storage, fileFilter })`
- **뭐 하는 함수?**: 업로드 설정을 담은 "우체부" 객체를 만든다
```js
const upload = multer({
  storage: multer.diskStorage({ ... }),
  fileFilter: (req, file, cb) => { ... }
});
```

### F2. `multer.diskStorage({ destination, filename })`
- **뭐 하는 함수?**: 파일을 어디에, 무슨 이름으로 저장할지 정한다
```js
storage: multer.diskStorage({
  destination: 'uploads/',                     // 저장 폴더
  filename: (req, file, cb) => {
    const ext = path.extname(file.originalname); // 원본 확장자 (.png 등)
    cb(null, Date.now() + '-' + Math.round(Math.random() * 1e9) + ext);
  }
})
```
> 파일명을 "현재시각-랜덤숫자.확장자"로 만들어 중복 방지 + 확장자 보존

### F3. `upload.single('필드명')`
- **뭐 하는 함수?**: 요청에서 파일 1개를 받아서 `req.file`에 넣어주는 미들웨어
```js
app.post('/upload', upload.single('image'), (req, res) => { ... });
```
> 폼의 `<input type="file" name="image">` 와 이름이 같아야 함

### F4. `fileFilter`
- **뭐 하는 함수?**: 어떤 파일만 받을지 검사 (여기선 이미지만 허용)
```js
fileFilter: (req, file, cb) => {
  if (!file.mimetype.startsWith('image/')) {
    return cb(new Error('이미지 파일만 업로드할 수 있습니다.'));
  }
  cb(null, true);  // 통과!
}
```

---

## G. 데이터베이스 (sqlite3)

### G1. `new sqlite3.Database('backup.db')`
- **뭐 하는 함수?**: DB 파일을 연다 (없으면 자동 생성)
- **비유**: 엑셀 파일을 더블클릭해서 여는 것
```js
const db = new sqlite3.Database('backup.db');
```

### G2. `db.run(SQL, 값들, 콜백)`
- **뭐 하는 함수?**: DB에 "쓰기" 명령 실행 (테이블 만들기, 추가, 수정)
- **비유**: 엑셀에 입력/수정하는 것
```js
db.run(`CREATE TABLE IF NOT EXISTS images (...)`);
db.run(`INSERT INTO images (...) VALUES (?, ?, ?, ?)`,
       [filename, originalname, title, description], function (err) { ... });
```

### G3. `db.all(SQL, 값들, 콜백)`
- **뭐 하는 함수?**: DB에서 "읽기" — 여러 줄을 통째로 가져옴
- **비유**: 엑셀에서 전체 행을 읽어오는 것
```js
db.all(`SELECT * FROM images ORDER BY id DESC`, [], (err, rows) => {
  res.json(rows);  // rows = 이미지 목록
});
```

---

## H. SQL 문법

| 문법 | 뜻 | 비유 |
|------|-----|------|
| `CREATE TABLE IF NOT EXISTS` | 테이블(표) 만들기. 이미 있으면 안 만듦 | 빈 엑셀 시트 생성 |
| `INTEGER` | 정수형 (id 같은 숫자) | 숫자 칸 |
| `TEXT` | 글자형 | 글자 칸 |
| `PRIMARY KEY AUTOINCREMENT` | 각 행의 고유 번호, 자동 1씩 증가 | 출석번호 자동 발급 |
| `DEFAULT '값'` | 값 안 넣으면 기본값 사용 | "빈칸이면 이걸로 채움" |
| `DATETIME DEFAULT CURRENT_TIMESTAMP` | 현재 시각 자동 저장 | 저장 시각 자동 기록 |
| `INSERT INTO ... VALUES` | 한 줄 추가 | 엑셀에 행 추가 |
| `SELECT * FROM ...` | 모든 컬럼 조회 | 전체 표 복사 |
| `ORDER BY id DESC` | id 기준 내림차순(최신순) | 최신 글이 위로 |
| `?` (플레이스홀더) | 값 자리에 미리 ? 넣고 배열로 전달 | "여기에 값을 넣을게" (SQL 인젝션 방지!) |

```sql
-- 테이블 만들기
CREATE TABLE IF NOT EXISTS images (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filename TEXT,
    original_name TEXT,
    title TEXT DEFAULT '제목 없음',
    description TEXT DEFAULT '',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 한 줄 추가
INSERT INTO images (filename, original_name, title, description)
VALUES (?, ?, ?, ?);

-- 전부 읽기 (최신순)
SELECT * FROM images ORDER BY id DESC;
```

---

## I. 파일/경로 (Node 기본)

### I1. `path.join(경로1, 경로2)`
- **뭐 하는 함수?**: 경로를 안전하게 합친다 (OS별 구분자 자동 처리)
```js
path.join(__dirname, 'public/index.html');
```
> `__dirname` = 현재 파일이 있는 폴더 경로

### I2. `path.extname(파일명)`
- **뭐 하는 함수?**: 파일명에서 확장자(.png 등)를 뽑는다
```js
const ext = path.extname('cat.png');  // ".png"
```

### I3. `fs.existsSync('폴더')`
- **뭐 하는 함수?**: 폴더/파일이 존재하는지 확인 (true/false)
```js
if (!fs.existsSync('uploads')) fs.mkdirSync('uploads');
```

### I4. `fs.mkdirSync('폴더')`
- **뭐 하는 함수?**: 폴더를 만든다
```js
fs.mkdirSync('uploads');
```

---

## J. 프론트엔드 (브라우저 JS)

### J1. `fetch('주소')`
- **뭐 하는 함수?**: 브라우저가 서버에 요청 보내고 응답 받기
- **비유**: 손님이 주방에 "주문한 요리 나왔나요?" 물어보는 것
```js
fetch('/api/images')
  .then(res => res.json())   // 응답을 JSON으로 변환
  .then(data => { ... });    // 받은 데이터로 작업
```

### J2. `document.getElementById('아이디')`
- **뭐 하는 함수?**: HTML에서 특정 아이디의 요소를 찾는다
```js
const gallery = document.getElementById('gallery');
```

### J3. `요소.innerHTML = 'HTML문자열'`
- **뭐 하는 함수?**: 요소 안에 HTML을 넣는다 (화면에 그리기)
```js
gallery.innerHTML = '';
```

### J4. `document.createElement('태그')`
- **뭐 하는 함수?**: HTML 요소를 새로 만든다
```js
const card = document.createElement('div');

### J5. `배열.forEach(함수)`
- **뭐 하는 함수?**: 배열의 각 요소마다 함수 실행
- **비유**: 리스트를 하나씩 꺼내며 같은 작업 반복
```js
data.forEach(img => {
  // img 하나마다 카드 만들기
});
```
> `=>` (화살표 함수) = 파이썬의 `lambda` 느낌. `(x) => x * 2` 같은 것

---

## K. HTML 문법 (브라우저 화면)

> HTML = 화면에 뭘 그릴지 적는 **문서 양식**. `<태그>`로 시작해서 `</태그>`로 닫는 게 기본 규칙.

### K1. 기본 구조
- **뭐 하는 것?**: 모든 HTML 파일의 뼈대
```html
<!DOCTYPE html>      <!-- 이 문서는 HTML이다 (선언) -->
<html>               <!-- 전체 시작 -->
  <head>             <!-- 설정칸: 제목, 스타일 등 -->
    <title>제목</title>
  </head>
  <body>             <!-- 화면에 보이는 내용칸 -->
    ...
  </body>
</html>
```

### K2. `<form action="주소" method="POST">` — 신호를 보내는 폼
- **뭐 하는 것?**: 사용자 입력을 묶어서 **action에 적힌 주소로 자동 전송**하는 상자
- **비유**: 제출용 서류 봉투 (봉투 앞면에 보낼 주소=action, 방법=method가 적혀 있음)
```html
<form action="/upload" method="POST" enctype="multipart/form-data">
  <!-- 여기 안의 input/textarea 값들이 자동으로 모여서 전송됨 -->
</form>
```
> `method="POST"` = 보내기 / `method="GET"` = 조회 (주소창에 보임)
> `enctype="multipart/form-data"` = 파일도 함께 보낼 때 꼭 필요

### K3. `<input>` — 입력 칸 (한 줄)
- **뭐 하는 것?**: 사용자가 값/파일을 넣는 칸
- **비유**: 서류의 빈칸
```html
<input type="text" name="title" />          <!-- 한 줄 텍스트 -->
<input type="file" name="image" />          <!-- 파일 선택 -->
```
> `name` = 이 값을 뭐라고 부를지. 서버에서 `req.body.title`처럼 읽음. **폼 제출 때 name이 있어야 값이 전송됨!**
> `type` = 칸의 종류 (text, file, password 등)

### K4. `<textarea>` — 여러 줄 입력 칸
- **뭐 하는 것?**: 긴 글(설명 등)을 넣는 칸
```html
<textarea name="description">...</textarea>
```

### K5. `<button type="submit">` — 폼을 보내는 버튼
- **뭐 하는 것?**: 클릭하면 **자기 주변 `<form>`의 내용을 action 주소로 전송**하는 버튼
- **비유**: "이 봉투 보내!" 버튼
```html
<button type="submit">작품 업로드</button>
```
> **핵심 규칙**: `type="submit"` 버튼이 `<form>` 안에 있으면 → 브라우저가 알아서 그 폼을 전송한다. (JS 필요 없음!)

### K6. `<div>` — 그룹 상자
- **뭐 하는 것?**: 여러 요소를 묶는 투명한 상자 (레이아웃/꾸미기용)
```html
<div class="card"> ... </div>
```
> class/id = 이름표. JS에서 찾거나 CSS로 꾸밀 때 사용

### K7. `<img src="주소">` — 이미지 표시
- **뭐 하는 것?**: 사진을 화면에 보여줌
```html
<img src="/uploads/abc.png" alt="이미지">
```
> `src` = 이미지 파일 주소 / `alt` = 이미지가 안 보일 때 대신 보여줄 글

### K8. `<script>` — JS 코드 넣는 곳
- **뭐 하는 것?**: HTML 안에 자바스크립트 코드를 담는 태그
```html
<script>
  fetch('/api/images') ...
</script>
```

---

## L. 삭제 기능 (DELETE)

### L1. `app.delete('경로', 핸들러)`
- **뭐 하는 함수?**: "이 주소로 DELETE(삭제) 요청이 오면 이 동작을 해줘"
- **비유**: "이 물건 버려줘" 하는 신호. GET(조회)/POST(보내기)와 같은 요청 종류 중 하나
```js
app.delete('/api/images/:id', (req, res) => { ... });
```

### L2. `req.params.id` — 주소에 담긴 값 읽기
- **뭐 하는 함수?**: 주소(URL)의 `:id` 자리에 들어온 값을 읽는다
- **비유**: "3번 이미지 지워줘"라고 했을 때 그 **3**을 꺼내는 것
```js
// 주소: /api/images/3  →  req.params.id = "3"
const id = req.params.id;
```
> `:id` 처럼 주소에 `:이름`을 쓰면 그 자리의 값이 `req.params.이름`으로 들어온다

### L3. `db.get(SQL, 값들, 콜백)` — 한 줄만 읽기
- **뭐 하는 함수?**: DB에서 **딱 1줄**만 가져온다 (여러 줄은 `db.all`)
- **비유**: 엑셀에서 특정 행 1개만 찾는 것
```js
db.get(`SELECT * FROM images WHERE id = ?`, [id], (err, row) => {
  // row = 찾은 한 줄 (없으면 undefined)
});
```

### L4. `DELETE FROM ... WHERE` — 한 줄 삭제
- **뭐 하는 함수?**: DB 표에서 조건에 맞는 줄을 지운다
- **비유**: 엑셀에서 특정 행 삭제
```sql
DELETE FROM images WHERE id = ?;
```
> `WHERE` = "어떤 줄을" 고르는 조건. 없으면 **전부 삭제**되니 꼭 붙이기!

### L5. `fs.unlink(경로, 콜백)` — 파일 삭제
- **뭐 하는 함수?**: 실제 파일을 컴퓨터에서 지운다
- **비유**: 휴지통에 버리기
```js
fs.unlink(path.join('uploads', row.filename), (err) => { ... });
```

### L6. `err.code === 'ENOENT'` — "파일 없음" 에러
- **뭐 하는 것?**: 파일이 이미 없을 때 나는 에러 코드
- **비유**: "이미 휴지통에 없는 걸 또 버리려 함" → 무시해도 됨
```js
if (unlinkErr && unlinkErr.code !== 'ENOENT') {
  // 진짜 에러일 때만 처리. 이미 없으면(ENOENT) 무시
}
```

### L7. `fetch('주소', { method: 'DELETE' })` — 삭제 요청 보내기
- **뭐 하는 함수?**: 브라우저가 서버에 DELETE 요청을 보낸다
- **비유**: 손님이 주방에 "이거 취소/삭제해줘" 하는 것
```js
fetch('/api/images/' + id, { method: 'DELETE' })
  .then(res => res.json())
  .then(data => { ... });
```

### L8. `요소.addEventListener('click', 함수)` — 클릭 감지
- **뭐 하는 함수?**: 요소를 클릭했을 때 함수를 실행시킨다
- **비유**: 버튼에 "누르면 이렇게 해" 라는 지시를 붙여두는 것
```js
btn.addEventListener('click', () => { ... });
```

### L9. `요소.dataset.id` — 버튼에 숨겨둔 값 읽기
- **뭐 하는 함수?**: HTML에 `data-id="값"` 으로 적어둔 값을 읽는다
- **비유**: 버튼에 "나는 3번 이미지야" 라는 이름표를 붙여두는 것
```html
<button data-id="3">삭제</button>
```
```js
const id = btn.dataset.id;  // "3"
```

### L10. `confirm('문구')` — 확인창 띄우기
- **뭐 하는 함수?**: "정말?" 하고 물어보는 창. 확인=true, 취소=false
```js
if (!confirm('정말 삭제할까요?')) return; // 취소하면 여기서 멈춤
```

### L11. `location.reload()` — 페이지 새로고침
- **뭐 하는 함수?**: 현재 페이지를 다시 불러온다 (최신 목록 보기)
```js
location.reload();
```

---

## 🧠 꿀팁 정리

- `?` 플레이스홀더로 SQL 값 바인딩 → 문자열 조합(SQL 인젝션) 금지!
- 파일명은 서버가 새로 만들어서 저장 (원본명은 DB에만 보관)
- 이미지 파일(uploads/)과 정보(backup.db)는 저장 장소가 다르다
- **삭제도 파일 + DB 둘 다 해야 한다** (데이터 일관성!)
- `WHERE` 없이 `DELETE` 하면 **전부 삭제**되니 항상 조건 붙이기
- 에러 핸들러 `app.use((err, req, res, next) => ...)` 는 맨 마지막에!

---

## M. 상세 페이지 (페이지 이동 & 주소 값)

### M1. 페이지 라우트 vs 데이터 API
- **뭐 하는 것?**: "화면(HTML) 보여주기"와 "데이터(JSON) 꺼내기"는 서로 다른 라우트가 담당
- **비유**: 페이지 = 빈 서류 양식, 데이터 API = 그 서류에 채울 실제 내용
```js
app.get('/image/:id', ...)        // 상세 "페이지"(HTML)를 보내줌. DB 접근 안 함
app.get('/api/images/:id', ...)   // 상세 "데이터"(JSON)를 보내줌. DB에서 꺼냄
```
> 상세 페이지는 빈 화면(HTML)을 받고, 그 안의 JS가 다시 데이터 API를 호출해 화면을 채운다.

### M2. `location.pathname.split('/').pop()` — 주소에서 id 꺼내기 (브라우저)
- **뭐 하는 함수?**: 현재 주소에서 마지막 조각(예: id)을 꺼낸다
- **비유**: 주소를 `/` 기준으로 잘라서 마지막 조각만 집는 것
```js
// 주소: /image/6  →  pathname = "/image/6"
// split('/')  →  ["", "image", "6"]
// .pop()      →  "6"
const id = location.pathname.split('/').pop();
```
> `split('구분자')` = 문자열을 구분자 기준으로 잘라 배열로 만듦 / `.pop()` = 배열의 마지막 요소를 꺼냄

### M3. `location.href = '주소'` — 페이지 이동
- **뭐 하는 함수?**: 브라우저를 다른 주소로 이동시킨다
- **비유**: "저쪽 창구로 가세요" 하고 손님을 안내하는 것
```js
location.href = '/image/' + img.dataset.id;  // /image/3 으로 이동
```

### M4. `req.params` — 주소의 `:이름` 값들 (서버)
- **뭐 하는 함수?**: 라우트에서 `:이름`으로 표시한 자리에 온 값들을 모아둔 상자
- **비유**: 예약 시스템. "예약번호 :번호로 오면" → 손님이 "7번"으로 오면 req.params.번호 = "7"
```js
app.get('/image/:id', (req, res) => {
  console.log(req.params);   // { id: "3" }
  console.log(req.params.id); // "3"
});
```
> `:` 뒤의 이름은 내가 정함. `:num`이라면 `req.params.num`이 됨.

### M5. `req` 안에 들어있는 것들
- **뭐 하는 것?**: 요청에 대한 정보가 담긴 "주문서 봉투"
- **비유**: req.params = 봉투 겉면 주소, req.body = 봉투 안 주문 내용, req.file = 동봉된 사진
```js
req.params  // 주소의 :이름 값들  (예: { id: "3" })
req.body    // 폼에서 보낸 값들   (예: { title: "강아지" })
req.file    // 업로드된 파일 정보 (multer가 넣어줌)
req.query   // 주소의 ?뒤 값들    (아직 안 씀)
```

### M6. `addEventListener('이벤트', 함수)` — 이벤트 감지
- **뭐 하는 함수?**: 요소에서 이벤트(클릭 등)가 발생하면 함수를 실행시킨다
- **비유**: 버튼에 "누르면 이렇게 해" 라는 지시를 붙여두는 것
```js
img.addEventListener('click', () => {
  location.href = '/image/' + img.dataset.id;
});
```
> 두 번째 인자 = "이벤트가 발생했을 때 실행할 함수". "언제(이벤트) → 뭐 할래(함수)".
