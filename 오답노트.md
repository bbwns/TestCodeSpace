# 📓 오답노트 (나만의 학습 기록장)

> 질문해서 배운 걸 간단히 Q./A.로 하나씩 기록한다.

## 📝 기록

### Q. `existsSync`, `mkdirSync`의 Sync는 뭐야?
A. Sync = **동기(Synchronous)** = "이 작업이 끝날 때까지 **기다린다**"는 뜻.
   이 프로젝트에선 폴더를 만들고 나서 파일을 저장해야 하니까 (전제조건) 기다리는 Sync를 쓴다.
   비유: Sync 없음 = "밥 시키고 딴짓", Sync = "밥 나올 때까지 기다림".

### Q. `storage:`는 뭐야?
A. multer(우체부)에게 **"받은 파일을 어디에, 어떤 이름으로 보관할지"** 알려주는 설정.
   - `destination` = 어느 창고(폴더)에 → uploads/
   - `filename` = 어떤 이름표로 → 함수로 이름을 정한다.

### Q. `filename: (req, file, cb)` 에서 req, file, cb는 뭐야?
A. 이름표를 정하는 함수가 받는 정보 3개.
   - `req` = 요청 정보 전체 (주문서: 제목, 설명 등)
   - `file` = 받은 파일 정보 (물건 정보: 원본명, 크기 등)
   - `cb` = 콜백, "이 이름으로 저장해!" 하고 우체부에게 최종 보고하는 함수.
     정상: `cb(null, "이름")` / 에러: `cb(new Error(...))`

### Q. `:` (콜론)은 뭔 역할이야?
A. **"이름(키): 값" 짝을 연결**하는 기호. 파이썬 `{"storage": ...}` 딕셔너리와 같음.
   `storage: multer.diskStorage({...})` = "storage라는 이름의 값은 이거다" 라는 뜻.

### Q. req가 지역변수인데 filename이랑 fileFilter에서 어캐 또 쓰여? (공유 아님?)
A. 맞아, **요청(지역변수) 맞고, 함수마다 각자의 변수**야. 이름만 같고 서로 다른 것.
   multer가 각 함수를 부를 때 **그 함수의 req 자리에 값을 넣어줌** (주문서를 직원들한테 각자 나눠주는 것).
   filename의 req와 fileFilter의 req는 서로 다른 함수의 매개변수일 뿐.

### Q. multer는 형식(함수 모음집)이고 실제로는 upload로 쓰는 거야? (클래스랑 비슷?)
A. 맞아! **multer = 도구함(라이브러리), upload = 그걸로 만들어낸 실전 도구.**
   - `const upload = multer({설정})` → multer를 한 번 호출해서 "설정된 업로드 도구"를 만들어 upload에 담음
   - 실제로는 `upload.single('image')` 로 일 시킴 (미들웨어)
   - 비유: multer=우체국(설계도/공장), upload=규칙 적용된 전담 우체부(실체), upload.single=실제 업무.
   - **클래스/인스턴스 개념과 같음**: multer=설계도(클래스), upload=설계도로 찍어낸 실체(인스턴스). 정확히는 "설계도로 실물을 만드는 함수(공장)" 느낌이지만, "틀→실물 생성→사용" 구조가 동일.

### Q. `app.get('/', (req, res) => {...})` 은 왜 get '/'랑 req, res를 쓰는 거야?
A. **라우트 등록**: "이 주소로 조회(GET) 요청이 오면 이 함수를 실행해." `/`는 메인(홈) 주소, `get`은 조회 요청만 받는 것.
   **req, res는 express가 함수를 부를 때 건네주는 두 자리**(매개변수):
   - `req` = 요청(주문서: 뭘 원하는지)
   - `res` = 응답(답장 도구: 우리가 보낼 것)
   우리는 `res.sendFile(HTML)`로 답장 도구에 문서를 담아 보냄.
   req를 안 써도 둘 다 적는 건 **관습(기본 틀)** — express가 항상 둘 다 건네주기 때문. 비유: 손님 주문(req)이 오면 express가 요리사(함수)를 부르며 주문서와 접시(res)를 건네주고, 우리는 접시에 음식을 담아 내놓는 것.

### Q. app.post, app.use 같은 건 어디서 쏘는 거야? 조건문 같은 거야? 항상 켜져서 프론트 신호 받는 거야?
A. **거의 정확!** 앱.get/post는 **바로 실행 안 되고 "미리 반응 조건을 등록"**만 해둠 (메뉴판).
   - 서버는 `app.listen`으로 **항상 대기**
   - **신호를 쏘는 건 프론트(브라우저)**: 버튼 클릭, fetch, 주소 입력
   - 요청이 오면 express가 **조건 매칭**(네 말대로 거대한 if문)으로 맞는 핸들러 실행
   - get=조회(GET)만, post=보내기(POST)만, use=조건 없이 모든 요청에 관통(미들웨어)

### Q. `app.get('/api/images', ...)`는 ~/api/images로 가는 버튼을 누르는 거야?
A. **아니요.** "그 주소로 GET 요청이 오기만 하면" 반응하는 거고, 우리는 **버튼 이동이 아니라 페이지 로딩 시 자동 실행되는 `fetch('/api/images')`**로 쏨.
   - 버튼/주소 이동: 화면이 통째로 바뀌고 JSON 텍스트만 보임 (원치 않음)
   - fetch: 화면 전환 없이 뒤에서 데이터만 가져와 JS로 갤러리 그림 (우리 방식)
   - `/api/images`는 "화면용 페이지"가 아니라 **"데이터(JSON)를 주는 API"**라 주소 이동으로 들어가면 안 됨.

### Q. index.html엔 업로드 버튼이 따로 뭘 보내는 JS가 없네?
A. **업로드 버튼("작품 업로드")은 있지만, JS로 보내는 게 아니라 HTML 폼이 자체적으로 보냄.**
   - `<button type="submit">` 이 `<form action="/upload" method="POST">` 안에 있어서, **브라우저가 알아서** 파일+제목+설명을 POST /upload로 제출 (JS/fetch 불필요)
   - 그래서 JS가 안 보이는 게 정상!
   - 페이지엔 두 방식: **업로드 = HTML 폼 제출**(버튼, JS 없음) / **갤러리 = fetch**(버튼 없음, 페이지 로딩 시 자동)

### Q. req.body.title은 req의 body 안 title 칸의 값이고, post()의 req, res는 =>{} 안으로 들어가는 값이다?
A. **맞아!**
   - `req.body.title` = req(주문서 봉투)의 body(작성 내용 칸) 안에 있는 **title이라는 이름표(칸)의 값**. 이름표는 HTML 폼의 `name="title"`이 정함.
   - `(req, res) => { ... }` = express가 함수를 부를 때 **req, res 값을 그 괄호 안으로 넣어줌** (받는 자리).
   - 요약: 괄호는 "선언(받기)"과 "호출(실행)"할 때만. 값을 읽는 `req.body.title`은 괄호 없이 그냥 속성 접근.
   